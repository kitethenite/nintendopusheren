<section class="container">
  <div class="baggrund">
    <h1 id="om_os">Om os</h1>
  </div>
  <img src="/images/about-top-720x640.webp" alt="" />
</section>

<style>
  @media only screen and (min-width: 768px) {
    .container {
      display: grid;
      grid-template-columns: 1fr 1fr;
      margin: 0;
      justify-items: stretch;
    }

    #om_os {
      margin-right: 100px;
      font-family: var(--font-family-primary);
      font-size: var(--font-size-xl);
      color: var(--primary-color);
    }

    img {
      grid-column: 2;
    }

    .baggrund {
      display: grid;
      align-items: center;
      justify-items: end;
      grid-column: 1/2;
      background-color: var(--secondary-accent);
    }
  }

  img {
    max-width: 100%;
    height: auto;
  }

  @media only screen and (max-width: 768px) {
    .baggrund {
      display: block;
      text-align: center;
      padding: 10px;
      background-color: var(--secondary-accent);
    }
    .container {
      display: block;
      width: 100%;
      background-color: var(--secondary-accent);
    }
    #om_os {
      font-family: var(--font-family-primary);
      font-size: var(--font-size-xl);
      color: var(--primary-color);
    }
  }
</style>
